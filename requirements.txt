# Web-Framework + ASGI-Server
fastapi>=0.110
uvicorn[standard]>=0.29

# Datenbank (SQLModel = SQLAlchemy + Pydantic)
sqlmodel>=0.0.16

# E-Mail-Validierung für pydantic EmailStr
email-validator>=2.0

# Konfiguration aus .env laden
python-dotenv>=1.0

# Google Wallet: JWT signieren (RS256 braucht 'cryptography')
PyJWT>=2.8
cryptography>=42.0

# QR-Code für das kostenlose Ticket (erzeugt PNG lokal, kein externer Dienst)
qrcode[pil]>=7.4

# Postgres-Treiber (für dauerhafte Datenbank, z. B. Neon). SQLite braucht ihn
# nicht, aber installiert schadet er nicht.
psycopg2-binary>=2.9
